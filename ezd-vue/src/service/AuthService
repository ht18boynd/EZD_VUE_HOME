import axios from "axios";

const axiosInstance = axios.create({
    headers: {
      'Cache-Control': 'no-cache',
    },
  });

  const AUTH_API_BASE_URL = 'http://localhost:8081/api/v1/auth/';

  class AuthService{
    
  };

  export default new AuthService();